const NotCompatible = () => {
  return (
    <div className="w-screen h-screen flex justify-center items-center">
      This website is only accessible via Chrome on desktop devices.
    </div>
  );
};

export default NotCompatible;
